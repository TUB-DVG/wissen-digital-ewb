"""Configuration module for the app `pages`

"""
from django.apps import AppConfig


class PagesConfig(AppConfig):
    """Configuration class for the app `pages`

    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "pages"
