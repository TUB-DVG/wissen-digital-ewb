"""django generated configuration script for the common app.

"""
from django.apps import AppConfig


class CommonConfig(AppConfig):
    """Class definition of the configuration class for the common app

    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "common"
